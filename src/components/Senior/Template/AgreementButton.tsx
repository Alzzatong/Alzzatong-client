export default function AgreementButton() {
    return (
      <button
        type="button"
        className="rounded bg-white px-3 py-2 text-sm font-normal text-zinc-500 font-['Pretendard'] leading-tight shadow-sm ring-1 ring-inset ring-zinc-400 hover:bg-gray-100"
      >
        개인정보동의서 (파일)
      </button>
    );
  }